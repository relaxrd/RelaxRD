Dataset: fraudTrain
CoreAFD pkl: CoreAFD/output/fraudTrain_coreafd.pkl
Original SQL file: Query_Performance/Workload/Original/fraudTrain/fraudTrain.sql
Main table (R_k+1): r4
Key columns: ['trans_num']

=== Original Schema ===
r0(amt, category, cc_num, city, city_pop, dob, first, gender, is_fraud, job, last, lat, long, merch_lat, merch_long, merchant, state, street, trans_date_trans_time, trans_num, unix_time, zip)

=== Decomposed Schemas ===
r1(cc_num, city, city_pop, dob, first, is_fraud, job, last, lat, long, state, street, zip)
r2(first, gender)
r3(category, merchant)
r4(amt, cc_num, merch_lat, merch_long, merchant, trans_date_trans_time, trans_num, unix_time)

=== SQL Rewriting Details ===
--- Statement 1 ---
[Original]
-- =========================================================
-- FRAUD workload for query performance evaluation
-- Single large table version: fraud_transactions
-- =========================================================


-- =========================================================
-- No-Join Queries
-- =========================================================

-- Q1:
-- Business scenario:
-- Retrieve transaction records under one indexed-like foreign-key attribute,
-- e.g., when inspecting all transactions issued by one specific card.
SELECT
    `trans_num`,
    `trans_date_trans_time`,
    `cc_num`,
    `merchant`,
    `amt`,
    `unix_time`
FROM fraud_transactions
WHERE `cc_num` = '4000000000000001';

[Rewritten]
-- =========================================================
-- FRAUD workload for query performance evaluation
-- Single large table version: fraud_transactions
-- =========================================================
-- =========================================================
-- No-Join Queries
-- =========================================================
-- Q1:
-- Business scenario:
-- Retrieve transaction records under one indexed-like foreign-key attribute,
-- e.g., when inspecting all transactions issued by one specific card.
WITH r_original AS (
    SELECT `amt`, `cc_num`, `merchant`, `trans_date_trans_time`, `trans_num`, `unix_time`
    FROM r4
    UNION ALL
    SELECT `amt`, `cc_num`, `merchant`, `trans_date_trans_time`, `trans_num`, `unix_time`
    FROM r0
)
SELECT
    `trans_num`,
    `trans_date_trans_time`,
    `cc_num`,
    `merchant`,
    `amt`,
    `unix_time`
FROM fraud_transactions
WHERE `cc_num` = '4000000000000001';

================================================================================

--- Statement 2 ---
[Original]
-- Q2:
-- Business scenario:
-- Inspect one lookup-style entry,
-- e.g., when checking the category associated with one merchant.
SELECT DISTINCT
    `merchant`,
    `category`
FROM fraud_transactions
WHERE `merchant` = 'fraud_Rutherford-Mertz';

[Rewritten]
-- Q2:
-- Business scenario:
-- Inspect one lookup-style entry,
-- e.g., when checking the category associated with one merchant.
WITH r_original AS (
    SELECT `category`, `merchant`
    FROM r3
    UNION ALL
    SELECT `category`, `merchant`
    FROM r0
)
SELECT DISTINCT
    `merchant`,
    `category`
FROM fraud_transactions
WHERE `merchant` = 'fraud_Rutherford-Mertz';

================================================================================

--- Statement 3 ---
[Original]
-- Q3:
-- Business scenario:
-- Filter transactions on a non-FK regular attribute,
-- e.g., when profiling medium-to-large transactions by amount.
SELECT
    `trans_num`,
    `trans_date_trans_time`,
    `merchant`,
    `amt`
FROM fraud_transactions
WHERE `amt` BETWEEN 100 AND 300;

[Rewritten]
-- Q3:
-- Business scenario:
-- Filter transactions on a non-FK regular attribute,
-- e.g., when profiling medium-to-large transactions by amount.
WITH r_original AS (
    SELECT `amt`, `merchant`, `trans_date_trans_time`, `trans_num`
    FROM r4
    UNION ALL
    SELECT `amt`, `merchant`, `trans_date_trans_time`, `trans_num`
    FROM r0
)
SELECT
    `trans_num`,
    `trans_date_trans_time`,
    `merchant`,
    `amt`
FROM fraud_transactions
WHERE `amt` BETWEEN 100 AND 300;

================================================================================

--- Statement 4 ---
[Original]
-- =========================================================
-- Queries corresponding to join-based lookups in decomposed schema
-- =========================================================

-- Q4:
-- Business scenario:
-- Decode customer metadata,
-- e.g., when translating a first name into gender information.
SELECT DISTINCT
    `cc_num`,
    `first`,
    `gender`,
    `state`,
    `zip`
FROM fraud_transactions
WHERE `first` = 'Jennifer';

[Rewritten]
-- =========================================================
-- Queries corresponding to join-based lookups in decomposed schema
-- =========================================================
-- Q4:
-- Business scenario:
-- Decode customer metadata,
-- e.g., when translating a first name into gender information.
WITH r_original AS (
    SELECT `cc_num`, `first`, `gender`, `state`, `zip`
    FROM r1
    JOIN r2 USING (`first`)
    UNION ALL
    SELECT `cc_num`, `first`, `gender`, `state`, `zip`
    FROM r0
)
SELECT DISTINCT
    `cc_num`,
    `first`,
    `gender`,
    `state`,
    `zip`
FROM fraud_transactions
WHERE `first` = 'Jennifer';

================================================================================

--- Statement 5 ---
[Original]
-- Q5:
-- Business scenario:
-- Reconstruct transaction records filtered by indexed-like attributes,
-- e.g., when inspecting all transactions of one card at one merchant.
SELECT
    `trans_num`,
    `trans_date_trans_time`,
    `cc_num`,
    `first`,
    `last`,
    `merchant`,
    `category`,
    `amt`
FROM fraud_transactions
WHERE `cc_num` = '4000000000000001'
  AND `merchant` = 'fraud_Rutherford-Mertz';

[Rewritten]
-- Q5:
-- Business scenario:
-- Reconstruct transaction records filtered by indexed-like attributes,
-- e.g., when inspecting all transactions of one card at one merchant.
WITH r_original AS (
    SELECT `amt`, `category`, `cc_num`, `first`, `last`, `merchant`, `trans_date_trans_time`, `trans_num`
    FROM r1
    JOIN r4 USING (`cc_num`)
    JOIN r3 USING (`merchant`)
    UNION ALL
    SELECT `amt`, `category`, `cc_num`, `first`, `last`, `merchant`, `trans_date_trans_time`, `trans_num`
    FROM r0
)
SELECT
    `trans_num`,
    `trans_date_trans_time`,
    `cc_num`,
    `first`,
    `last`,
    `merchant`,
    `category`,
    `amt`
FROM fraud_transactions
WHERE `cc_num` = '4000000000000001'
  AND `merchant` = 'fraud_Rutherford-Mertz';

================================================================================

--- Statement 6 ---
[Original]
-- Q6:
-- Business scenario:
-- Reconstruct transaction records with partially selective predicates,
-- e.g., when analyzing transactions of one indexed-like merchant together with
-- a non-key amount filter.
SELECT
    `trans_num`,
    `merchant`,
    `category`,
    `cc_num`,
    `first`,
    `gender`,
    `amt`
FROM fraud_transactions
WHERE `merchant` = 'fraud_Rutherford-Mertz'
  AND `amt` BETWEEN 100 AND 300;

[Rewritten]
-- Q6:
-- Business scenario:
-- Reconstruct transaction records with partially selective predicates,
-- e.g., when analyzing transactions of one indexed-like merchant together with
-- a non-key amount filter.
WITH r_original AS (
    SELECT `amt`, `category`, `cc_num`, `first`, `gender`, `merchant`, `trans_num`
    FROM r1
    JOIN r2 USING (`first`)
    JOIN r4 USING (`cc_num`)
    JOIN r3 USING (`merchant`)
    UNION ALL
    SELECT `amt`, `category`, `cc_num`, `first`, `gender`, `merchant`, `trans_num`
    FROM r0
)
SELECT
    `trans_num`,
    `merchant`,
    `category`,
    `cc_num`,
    `first`,
    `gender`,
    `amt`
FROM fraud_transactions
WHERE `merchant` = 'fraud_Rutherford-Mertz'
  AND `amt` BETWEEN 100 AND 300;

================================================================================

--- Statement 7 ---
[Original]
-- =========================================================
-- Analytical Queries
-- =========================================================

-- Q7:
-- Business scenario:
-- Summarize transaction volume by merchant,
-- e.g., to understand how transactions are distributed across merchants.
SELECT
    `merchant`,
    COUNT(*) AS cnt
FROM fraud_transactions
GROUP BY `merchant`
ORDER BY cnt DESC;

[Rewritten]
-- =========================================================
-- Analytical Queries
-- =========================================================
-- Q7:
-- Business scenario:
-- Summarize transaction volume by merchant,
-- e.g., to understand how transactions are distributed across merchants.
WITH r_original AS (
    SELECT `merchant`
    FROM r3
    UNION ALL
    SELECT `merchant`
    FROM r0
)
SELECT
    `merchant`,
    COUNT(*) AS cnt
FROM fraud_transactions
GROUP BY `merchant`
ORDER BY cnt DESC;

================================================================================

--- Statement 8 ---
[Original]
-- Q8:
-- Business scenario:
-- Measure average transaction amount by merchant category,
-- e.g., to compare transaction size across spending categories.
SELECT
    `category`,
    AVG(`amt`) AS avg_amt
FROM fraud_transactions
where amt>50
GROUP BY `category`
ORDER BY avg_amt DESC;

[Rewritten]
-- Q8:
-- Business scenario:
-- Measure average transaction amount by merchant category,
-- e.g., to compare transaction size across spending categories.
WITH r_original AS (
    SELECT `amt`, `category`
    FROM r3
    JOIN r4 USING (`merchant`)
    UNION ALL
    SELECT `amt`, `category`
    FROM r0
)
SELECT
    `category`,
    AVG(`amt`) AS avg_amt
FROM fraud_transactions
where amt>50
GROUP BY `category`
ORDER BY avg_amt DESC;

================================================================================

--- Statement 9 ---
[Original]
-- Q9:
-- Business scenario:
-- Identify the top states with the largest total suspicious exposure,
-- e.g., for geographic fraud-risk analysis.
SELECT
    `state`,
    SUM(`amt`) AS total_amt
FROM fraud_transactions
WHERE `is_fraud` = 1
GROUP BY `state`
ORDER BY total_amt DESC
LIMIT 10;

[Rewritten]
-- Q9:
-- Business scenario:
-- Identify the top states with the largest total suspicious exposure,
-- e.g., for geographic fraud-risk analysis.
WITH r_original AS (
    SELECT `amt`, `is_fraud`, `state`
    FROM r1
    JOIN r4 USING (`cc_num`)
    UNION ALL
    SELECT `amt`, `is_fraud`, `state`
    FROM r0
)
SELECT
    `state`,
    SUM(`amt`) AS total_amt
FROM fraud_transactions
WHERE `is_fraud` = 1
GROUP BY `state`
ORDER BY total_amt DESC
LIMIT 10;

================================================================================

--- Statement 10 ---
[Original]
-- =========================================================
-- Write Queries
-- =========================================================

-- Q10:
-- Business scenario:
-- Insert one newly arrived transaction record,
-- e.g., when a fresh card transaction is appended to the system.
INSERT INTO fraud_transactions (
    `trans_date_trans_time`,
    `cc_num`,
    `merchant`,
    `category`,
    `amt`,
    `first`,
    `last`,
    `gender`,
    `street`,
    `city`,
    `state`,
    `zip`,
    `lat`,
    `long`,
    `city_pop`,
    `job`,
    `dob`,
    `trans_num`,
    `unix_time`,
    `merch_lat`,
    `merch_long`,
    `is_fraud`
) VALUES (
    '2025-03-01 12:00:00',
    '4000000000000001',
    'fraud_Rutherford-Mertz',
    'shopping_pos',
    188.50,
    'Jennifer',
    'Smith',
    'F',
    '123 Test St',
    'Des Moines',
    'IA',
    '50309',
    41.5868,
    -93.6250,
    214133,
    'Scientist',
    '1985-05-01',
    'TXN999999001',
    1740830400,
    41.6000,
    -93.6200,
    0
);

[Rewritten]
-- =========================================================
-- Write Queries
-- =========================================================
-- Q10:
-- Business scenario:
-- Insert one newly arrived transaction record,
-- e.g., when a fresh card transaction is appended to the system.
INSERT INTO r0 (
    `trans_date_trans_time`,
    `cc_num`,
    `merchant`,
    `category`,
    `amt`,
    `first`,
    `last`,
    `gender`,
    `street`,
    `city`,
    `state`,
    `zip`,
    `lat`,
    `long`,
    `city_pop`,
    `job`,
    `dob`,
    `trans_num`,
    `unix_time`,
    `merch_lat`,
    `merch_long`,
    `is_fraud`
) VALUES (
    '2025-03-01 12:00:00',
    '4000000000000001',
    'fraud_Rutherford-Mertz',
    'shopping_pos',
    188.50,
    'Jennifer',
    'Smith',
    'F',
    '123 Test St',
    'Des Moines',
    'IA',
    '50309',
    41.5868,
    -93.6250,
    214133,
    'Scientist',
    '1985-05-01',
    'TXN999999001',
    1740830400,
    41.6000,
    -93.6200,
    0
);

================================================================================

--- Statement 11 ---
[Original]
-- Q11:
-- Business scenario:
-- Update one specific transaction record,
-- e.g., when the system corrects the transaction amount
-- or fraud label for a given transaction id.
UPDATE fraud_transactions
SET
    `amt` = 250.00,
    `is_fraud` = 1
WHERE `trans_num` = 'TXN999999001';

[Rewritten]
-- Q11:
-- Business scenario:
-- Update one specific transaction record,
-- e.g., when the system corrects the transaction amount
-- or fraud label for a given transaction id.
DELETE FROM r4
WHERE EXISTS (
    SELECT 1
    FROM (
        SELECT `amt`, `category`, `cc_num`, `city`, `city_pop`, `dob`, `first`, `gender`, `is_fraud`, `job`, `last`, `lat`, `long`, `merch_lat`, `merch_long`, `merchant`, `state`, `street`, `trans_date_trans_time`, `trans_num`, `unix_time`, `zip`
        FROM (
            SELECT `amt`, `category`, `cc_num`, `city`, `city_pop`, `dob`, `first`, `gender`, `is_fraud`, `job`, `last`, `lat`, `long`, `merch_lat`, `merch_long`, `merchant`, `state`, `street`, `trans_date_trans_time`, `trans_num`, `unix_time`, `zip`
            FROM r1
            JOIN r2 USING (`first`)
            JOIN r4 USING (`cc_num`)
            JOIN r3 USING (`merchant`)
            UNION ALL
            SELECT `amt`, `category`, `cc_num`, `city`, `city_pop`, `dob`, `first`, `gender`, `is_fraud`, `job`, `last`, `lat`, `long`, `merch_lat`, `merch_long`, `merchant`, `state`, `street`, `trans_date_trans_time`, `trans_num`, `unix_time`, `zip`
            FROM r0
        ) AS r_original
        WHERE `trans_num` = 'TXN999999001'
    ) AS src
    WHERE r4.`trans_num` = src.`trans_num`
);
DELETE FROM r0
WHERE `trans_num` = 'TXN999999001';
INSERT INTO r0 (`amt`, `category`, `cc_num`, `city`, `city_pop`, `dob`, `first`, `gender`, `is_fraud`, `job`, `last`, `lat`, `long`, `merch_lat`, `merch_long`, `merchant`, `state`, `street`, `trans_date_trans_time`, `trans_num`, `unix_time`, `zip`)
SELECT
    250.00 AS `amt`,
    src.`category` AS `category`,
    src.`cc_num` AS `cc_num`,
    src.`city` AS `city`,
    src.`city_pop` AS `city_pop`,
    src.`dob` AS `dob`,
    src.`first` AS `first`,
    src.`gender` AS `gender`,
    1 AS `is_fraud`,
    src.`job` AS `job`,
    src.`last` AS `last`,
    src.`lat` AS `lat`,
    src.`long` AS `long`,
    src.`merch_lat` AS `merch_lat`,
    src.`merch_long` AS `merch_long`,
    src.`merchant` AS `merchant`,
    src.`state` AS `state`,
    src.`street` AS `street`,
    src.`trans_date_trans_time` AS `trans_date_trans_time`,
    src.`trans_num` AS `trans_num`,
    src.`unix_time` AS `unix_time`,
    src.`zip` AS `zip`
FROM (
    SELECT `amt`, `category`, `cc_num`, `city`, `city_pop`, `dob`, `first`, `gender`, `is_fraud`, `job`, `last`, `lat`, `long`, `merch_lat`, `merch_long`, `merchant`, `state`, `street`, `trans_date_trans_time`, `trans_num`, `unix_time`, `zip`
    FROM (
        SELECT `amt`, `category`, `cc_num`, `city`, `city_pop`, `dob`, `first`, `gender`, `is_fraud`, `job`, `last`, `lat`, `long`, `merch_lat`, `merch_long`, `merchant`, `state`, `street`, `trans_date_trans_time`, `trans_num`, `unix_time`, `zip`
        FROM r1
        JOIN r2 USING (`first`)
        JOIN r4 USING (`cc_num`)
        JOIN r3 USING (`merchant`)
        UNION ALL
        SELECT `amt`, `category`, `cc_num`, `city`, `city_pop`, `dob`, `first`, `gender`, `is_fraud`, `job`, `last`, `lat`, `long`, `merch_lat`, `merch_long`, `merchant`, `state`, `street`, `trans_date_trans_time`, `trans_num`, `unix_time`, `zip`
        FROM r0
    ) AS r_original
    WHERE `trans_num` = 'TXN999999001'
) AS src;

================================================================================

--- Statement 12 ---
[Original]
-- Q12:
-- Business scenario:
-- Delete one specific transaction record,
-- e.g., when an invalid or duplicated transaction must be removed.
DELETE FROM fraud_transactions
WHERE `trans_num` = 'TXN999999001';

[Rewritten]
-- Q12:
-- Business scenario:
-- Delete one specific transaction record,
-- e.g., when an invalid or duplicated transaction must be removed.
DELETE FROM r4
WHERE EXISTS (
    SELECT 1
    FROM (
        SELECT DISTINCT `trans_num`
        FROM (
            SELECT `trans_num`
            FROM r4
            UNION ALL
            SELECT `trans_num`
            FROM r0
        ) AS r_original
        WHERE `trans_num` = 'TXN999999001'
    ) AS src
    WHERE r4.`trans_num` = src.`trans_num`
);
DELETE FROM r0
WHERE `trans_num` = 'TXN999999001';

================================================================================
